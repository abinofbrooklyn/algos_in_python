class Solution:
    def isOneBitCharacter(self, bits: List[int]) -> bool:
        bitsLen = len(bits)
        i = 0
        one = True
        while i < bitLens:
            one = True
            if bits[i] == 1:
                one = False
                i += 1
             i += 1
        return one
